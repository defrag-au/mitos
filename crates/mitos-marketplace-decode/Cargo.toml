[package]
name = "mitos-marketplace-decode"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "Shared marketplace (jpg.store / Wayup) redeemer + listing-datum decode for mitos. The single source of truth used by the *-store-sale community modules (live) and by cnft.dev-workers' historical backfill (Maestro/Koios-resolved txs) — decode once, so realtime and backfill can never disagree."

[lints.clippy]
disallowed_methods = "deny"

[dependencies]
pallas-codec.workspace = true
pallas-primitives.workspace = true
pallas-addresses.workspace = true
# Hash-verifies datum preimages recovered from jpg's tx metadata — the check
# that makes attacker-supplied metadata safe to read. See `metadata_datum`.
pallas-crypto.workspace = true
mitos-community-events = { path = "../mitos-community-events" }
hex.workspace = true
