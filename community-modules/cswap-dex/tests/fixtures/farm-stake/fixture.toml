# cswap-dex golden scenario: user stakes their LP tokens into
# a CSWAP yield farm — no pool address, no order address.
#
# TX: 0987f7fe39b3ed9bdbaab766f8ca8349c5b141a37f2e592d7b00f28d3a19a1d5
# Slot: 183609728
#
# Doesn't touch either CSWAP script address we filter for, so
# `at_address(POOL_SCRIPT_ADDR)` interest doesn't match and the
# module isn't dispatched. Expected: zero emissions
# (AppliedEmpty).
#
# Proves CSWAP farm activity doesn't leak through as DEX events
# at the recognition layer. Farm-staking surfaces as a future
# `DexAction::StakeRewardClaim` (Phase 4) when we cover that
# domain explicitly with a `cswap-farm` module or similar.

version = 1

[[interest]]
kind = "at_address"
address = "addr1z8ke0c9p89rjfwmuh98jpt8ky74uy5mffjft3zlcld9h7ml3lmln3mwk0y3zsh3gs3dzqlwa9rjzrxawkwm4udw9axhs6fuu6e"

[[interest]]
kind = "at_address"
address = "addr1z9xf82dwn6aaaftz6dnjslhkgu0pvtxrhfqsxqm8h42u7uggjrxwszhuqj73gufx56c8qwnuhvf2nw5dzdr5f50rqr5qt8sqcf"

[[interest]]
kind = "at_address"
address = "addr1vyw4d7mypuwvt54twjr49z9ctzl86wv3ee8sv7guuv5ft7gd7y66g"
