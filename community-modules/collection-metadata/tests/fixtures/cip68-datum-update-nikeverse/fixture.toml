# mitos-run fixture for collection-metadata against the full
# Nikeverse1501 CIP-68 lifecycle: initial ref-token mint
# (block 156189660) followed by a datum refresh
# (block 172076571). Reuses both blocks from
# `asset-metadata-update`'s nikeverse1501 fixture.
#
# Policy:    de79250af8caffc7a64645d86939159f665d4107c3f198562007bf32
# Ref asset: 000643b04e696b65766572736531353031 ("Nikeverse1501")
#
# Expected emission shape:
#   1. `SnapshotBegin` + `SnapshotEnd` — empty cold-start.
#   2. `MetadataEvent::Initial` from block 156189660 — first
#      sighting of the ref token, original datum.
#   3. `MetadataEvent::Updated` from block 172076571 — the
#      datum-update TX. `prior_version` carries the
#      pre-refresh version field; `entry.version` carries the
#      post-refresh version.
#
# This proves the symmetry of the Mekka fixture: ref-token
# datum updates ARE emitted as `Updated` events (not silently
# dropped, not double-emitted as `Initial`).

version = 1

[[interest]]
kind = "holds_policy"
policy = "de79250af8caffc7a64645d86939159f665d4107c3f198562007bf32"
